apiVersion: v1

kind: ConfigMap

metadata: 
    name: myapp-config
    
data: 
    PGUSER: "postgres"
    PGPORT: "5432"
    PGHOST: "postgres-service"
    PGDATABASE: "postgres"
    REDIS_PORT: "6379"
    REDIS_HOST: "redis-service"